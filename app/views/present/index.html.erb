<h1>My Project Presend </h1>
<% @photo.each do |img| %>
	<figure class="cap-bot">
	<%= link_to image_tag(url_for(controller: 'photo', action: 'photo_thumbnail', id: img.id)), controller: "present", action: "show", id: img.id %>
		<figcaption>hhahahahahahahahahah</figcaption>
	</figure>
<% end %>
